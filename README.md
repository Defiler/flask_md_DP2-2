# Flask website intro

```
pip install flask £ tipiski, ja mums vajag vienu biblioteku
pip install flask --user £ tikai tad ja nav tiesibu un pirmais izlido ar error
pip install -r reqirements.txt # parasti izmanto so
```